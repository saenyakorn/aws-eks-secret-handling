apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-api
spec:
  template:
    spec:
      containers:
        - name: gateway-api
          # just a placeholder for kustomize
          image: gateway-api
          volumeMounts:
            - name: gateway-api-secrets
              mountPath: /app
              readOnly: true
      volumes:
        - name: gateway-api-secrets
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: gateway-api-secrets
